# Base path for deployment (optional)
# Leave empty or unset to build at root (/)
# Set to /fonts, /f, /view, etc. for subdirectory deployment

# For root deployment (default if not set):
# BASE_PATH=

# For GitHub Pages subdirectory deployment:
# BASE_PATH=/fonts

# For custom subdirectory:
# BASE_PATH=/your-custom-path

# GitHub repository for CDN URLs (format: username/repo)
# Used for generating jsdelivr CDN links
NEXT_PUBLIC_GITHUB_REPOSITORY=viperadnan-git/fonts

# GitHub branch (optional, defaults to 'main')
NEXT_PUBLIC_GITHUB_BRANCH=main
